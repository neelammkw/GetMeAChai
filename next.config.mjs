/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.pixabay.com', 'avatars.githubusercontent.com','encrypted-tbn0.gstatic.com'],
    },
  };
  
  export default nextConfig;
  